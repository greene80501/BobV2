from bob.app_server.routes import agents, config, exec, files, legacy, realtime, review, tasks, threads, turns

ALL_ROUTE_MODULES = [
    legacy,
    config,
    threads,
    turns,
    files,
    exec,
    agents,
    tasks,
    review,
    realtime,
]

